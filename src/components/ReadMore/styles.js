import { StyleSheet } from "react-native";

const styles: any = StyleSheet.create({
  viewOpen: {
    // overflow: 'auto',
  },
  viewClosed: {
    overflow: 'hidden',
    height: 100,
  },
  button: {
    marginTop: 10,
  }
});

export default styles;
