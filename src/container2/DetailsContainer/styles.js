import { StyleSheet, Dimensions } from 'react-native'

import { Constants } from 'expo';


export default {
	image: {
		backgroundColor: '#ccc',
		flex: 1,
		resizeMode: 'cover',
		// position: 'absolute',
		width: '100%',
		height: 200,
		justifyContent: 'center',
		borderRadius: 2,
	}
}
